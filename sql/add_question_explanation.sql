-- =========================================================
-- Поле «Пояснение» к заданию варианта.
-- Учитель пишет пояснение (вручную или AI-черновик),
-- ученик видит ПОСЛЕ сдачи варианта на странице разбора.
-- =========================================================

ALTER TABLE test_variant_questions
  ADD COLUMN IF NOT EXISTS explanation TEXT;

-- Проверка
SELECT 'test_variant_questions.explanation ok' AS status;
