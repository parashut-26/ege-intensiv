-- =========================================================
-- Публикация варианта для учеников.
-- Учитель загружает PDF спокойно, редактирует, и потом
-- открывает «глазик» — только тогда ученики видят вариант
-- в своих «Доступных пробниках».
-- =========================================================

ALTER TABLE test_variants
  ADD COLUMN IF NOT EXISTS is_published BOOLEAN NOT NULL DEFAULT FALSE;

-- Проверка
SELECT 'is_published ok' AS status;
