-- =========================================================
-- Поле для комментария учителя к ученику.
-- Учитель в карточке ученика пишет свои заметки —
-- видны только учителю/куратору. Удобно для родительских PDF
-- и для своей собственной памяти.
-- =========================================================

ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS teacher_comment TEXT;

-- Проверка
SELECT 'profiles.teacher_comment ok' AS status;
